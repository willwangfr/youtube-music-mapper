# Python
__pycache__/
*.py[cod]
*$py.class
.env
venv/
.venv/

# Data files (large/private)
backend/music_data.json

# Google OAuth secrets - NEVER commit these
client_secret*.json
backend/oauth.json
oauth.json

# YouTube Music auth (contains session cookies) - NEVER commit
backend/browser.json
backend/browser.json.bak
browser.json

# Runtime user data (profiles, groups, listening history) - contains PII
backend/profiles/
backend/groups/

# Manual library pastes from YT Music page
youtube-music-liked-songs-*.txt
youtube-music-*.txt
*.paste.txt

# Playwright/test artifacts
.playwright-mcp/

# Last.fm similar-artists cache (runtime data)
backend/lastfm_similar_cache.json

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo

# Node (if any)
node_modules/
.env

# Enrichment caches (regenerable, and large)
backend/data/

# Personal listening library — regenerate by importing your own data.
# frontend/graph_data.sample.json ships instead so a fresh clone renders.
frontend/graph_data.json
frontend/graph_data.json.bak
frontend/graph_data.json.old
frontend-v2/graph_data.json

# Subagent-driven-development scratch (briefs, reports, review packages)
.superpowers/

# Screenshots — previous ones captured full desktops with personal data.
*.png

# Regenerable image-URL cache (rebuild: python fetch_artist_images.py)
backend/artist_images.json
